name: QuillIOS
options:
  bundleIdPrefix: com.digimata
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "6.0"
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    DEVELOPMENT_TEAM: "68562FABX7"

packages:
  WhisperKit:
    url: https://github.com/argmaxinc/WhisperKit.git
    from: 0.9.0
  # Raw llama.cpp binding (prebuilt XCFramework, zero transitive deps —
  # LocalLLMClient was rejected: its dep tree conflicts with WhisperKit's
  # swift-transformers pin on every published version).
  LlamaSwift:
    url: https://github.com/mattt/llama.swift.git
    from: 2.10184.0
  MarkdownUI:
    url: https://github.com/gonzalezreal/swift-markdown-ui.git
    from: 2.4.0
  # Diarizer only (speaker separation); ASR stays on WhisperKit.
  FluidAudio:
    url: https://github.com/FluidInference/FluidAudio.git
    from: 0.7.0

targets:
  QuillIOS:
    type: application
    platform: iOS
    sources:
      - path: QuillIOS/Sources
      - path: Shared
      - path: QuillIOS/Resources/Assets.xcassets
        buildPhase: resources
    dependencies:
      - package: WhisperKit
      - package: LlamaSwift
        product: LlamaSwift
      - package: MarkdownUI
        product: MarkdownUI
      - package: FluidAudio
        product: FluidAudio
      - target: QuillWidgets
        embed: true
    info:
      path: QuillIOS/Resources/Info.plist
      properties:
        CFBundleDisplayName: quill
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "18221475"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSMicrophoneUsageDescription: >-
          quill records your microphone so it can transcribe on-device.
          Audio never leaves this phone.
        UIFileSharingEnabled: true
        LSSupportsOpeningDocumentsInPlace: true
        UIBackgroundModes:
          - audio
        NSSupportsLiveActivities: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.digimata.quill-ios
        PRODUCT_NAME: quill
        TARGETED_DEVICE_FAMILY: "1"
        CODE_SIGN_STYLE: Automatic
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

  QuillWidgets:
    type: app-extension
    platform: iOS
    sources:
      - path: QuillWidgets
      - path: Shared
    info:
      path: QuillWidgets/Info.plist
      properties:
        CFBundleDisplayName: quill
        # Must match the app target's, or the build warns and an App Store
        # submission is rejected outright by ITMS validation. The plist is
        # generated from here, so keeping the two literals together is the
        # only place they can be kept in sync.
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "18221475"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.digimata.quill-ios.widgets
        CODE_SIGN_STYLE: Automatic
        SKIP_INSTALL: YES

  QuillIOSUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: QuillIOSUITests
    dependencies:
      - target: QuillIOS
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.digimata.quill-ios-uitests
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGN_STYLE: Automatic
        TEST_TARGET_NAME: QuillIOS

schemes:
  QuillIOS:
    build:
      targets:
        QuillIOS: all
        QuillIOSUITests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - QuillIOSUITests
